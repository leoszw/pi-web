import { useEffect, useMemo, useState } from 'react'
import type { P9EvalCase, P9EvalDomain, P9EvalFailureSummary, P9EvalObservation, P9EvalRunSummary, P9EvalVariant } from '../../../../shared/industry/eval/p9'
import { createP9ApiClient, type P9ApiClient } from '../../api/p9-client'
import './eval.css'

const defaultClient=createP9ApiClient()
export interface P9EvalSnapshot{cases:readonly P9EvalCase[];runs:readonly P9EvalRunSummary[];selectedRun?:P9EvalRunSummary;observations:readonly P9EvalObservation[];failures:readonly P9EvalFailureSummary[]}

export function P9EvalPage({domain,client=defaultClient,initialSnapshot}:{domain:P9EvalDomain;client?:P9ApiClient;initialSnapshot?:P9EvalSnapshot}){
  const[snapshot,setSnapshot]=useState<P9EvalSnapshot|undefined>(initialSnapshot);const[variantId,setVariantId]=useState<P9EvalVariant>('p9-guarded-v1');const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null)
  useEffect(()=>{if(initialSnapshot!==undefined)return undefined;let cancelled=false;void loadSnapshot(client,domain).then((value)=>{if(!cancelled)setSnapshot(value)}).catch((reason:unknown)=>{if(!cancelled)setError(messageOf(reason))});return()=>{cancelled=true}},[client,domain,initialSnapshot])
  async function start():Promise<void>{setBusy(true);setError(null);try{const selectedRun=await client.startEvalRun(domain,{datasetId:datasetId(domain),variantId});const[runs,observations,failures]=await Promise.all([client.listEvalRuns(domain),client.listEvalObservations(selectedRun.runId),client.listEvalFailures(selectedRun.runId)]);setSnapshot((value)=>({cases:value?.cases??[],runs,selectedRun,observations,failures}))}catch(reason){setError(messageOf(reason))}finally{setBusy(false)}}
  async function select(runId:string):Promise<void>{setBusy(true);setError(null);try{const[selectedRun,observations,failures]=await Promise.all([client.getEvalRun(runId),client.listEvalObservations(runId),client.listEvalFailures(runId)]);setSnapshot((value)=>value===undefined?value:{...value,selectedRun,observations,failures})}catch(reason){setError(messageOf(reason))}finally{setBusy(false)}}
  if(snapshot===undefined)return <main className="eval-page"><h1>{title(domain)}</h1><p>{error??'正在加载 P9 评测…'}</p></main>
  return <P9EvalView snapshot={snapshot} domain={domain} variantId={variantId} busy={busy} error={error} onVariantChange={setVariantId} onStart={()=>void start()} onSelect={(id)=>void select(id)}/>
}

export function P9EvalView({snapshot,domain,variantId,busy,error,onVariantChange=()=>undefined,onStart=()=>undefined,onSelect=()=>undefined}:{snapshot:P9EvalSnapshot;domain:P9EvalDomain;variantId:P9EvalVariant;busy:boolean;error:string|null;onVariantChange?:(value:P9EvalVariant)=>void;onStart?:()=>void;onSelect?:(id:string)=>void}){
  const run=snapshot.selectedRun;const byCase=useMemo(()=>new Map(snapshot.observations.map((item)=>[item.caseId,item])),[snapshot.observations])
  return <main className="eval-page" aria-labelledby="p9-eval-title"><div className="eval-eyebrow">评测工作台 · P9</div><div className="eval-heading-row"><div><h1 id="p9-eval-title">{title(domain)}</h1><p>{description(domain)}</p></div><div className="eval-heading-actions"><a href="/industry/eval/report">报告评测</a><a href="/industry/eval/sandbox">沙箱评测</a><a href={domain==='REPORT'?'/industry/reports':'/industry/sandbox'}>打开管理 UI</a><a href="/industry/eval">评测</a></div></div>{error===null?null:<p role="alert" className="eval-error">{error}</p>}
    <section className="eval-panel"><div className="eval-heading-row"><div><h2>批次运行</h2><p>数据集 <code>{datasetId(domain)}</code> · 确定性 P9 安全配置。</p></div><div className="eval-heading-actions"><select aria-label="P9 变体" value={variantId} disabled={busy} onChange={(event)=>onVariantChange(event.target.value as P9EvalVariant)}><option value="p9-broken-v0">p9-broken-v0</option><option value="p9-guarded-v1">p9-guarded-v1</option></select><button type="button" disabled={busy} onClick={onStart}>运行 {domain==='REPORT'?'报告':'沙箱'} 评测</button></div></div></section>
    {run===undefined?<section className="eval-panel"><p>选择或开始一次运行。</p></section>:<Metrics domain={domain} run={run}/>} 
    <section className="eval-panel"><h2>运行记录</h2><table className="eval-table"><thead><tr><th>运行</th><th>变体</th><th>项目</th><th>门禁</th><th>通过</th><th /></tr></thead><tbody>{snapshot.runs.map((item)=><tr key={item.runId} data-selected={run?.runId===item.runId}><td><code>{item.runId}</code></td><td>{item.variantId}</td><td><code>{item.projectId}</code></td><td>{item.metrics.releaseGate}</td><td>{item.metrics.passedCount}/{item.metrics.sampleCount}</td><td><button type="button" disabled={busy} onClick={()=>onSelect(item.runId)}>查看</button></td></tr>)}</tbody></table></section>
    <section className="eval-panel"><h2>用例</h2><table className="eval-table"><thead><tr><th>用例</th><th>场景</th><th>期望</th><th>结果</th></tr></thead><tbody>{snapshot.cases.map((testCase)=>{const observation=byCase.get(testCase.caseId);return <tr key={testCase.caseId}><td><strong>{testCase.title}</strong><br/><code>{testCase.caseId}</code></td><td>{testCase.scenario}</td><td>{testCase.expected}</td><td>{observation===undefined?'—':observation.passed?'PASS':'FAIL'}</td></tr>})}</tbody></table></section>
    <section className="eval-panel"><h2>失败下钻</h2>{snapshot.failures.length===0?<p>所选运行中没有失败的 P9 用例。</p>:snapshot.failures.map((failure)=>{const observation=snapshot.observations.find((item)=>item.caseId===failure.caseId);return <article className="eval-panel" key={failure.caseId} data-p9-failure={failure.caseId}><div className="eval-heading-row"><div><strong>{failure.title}</strong><p><code>{failure.caseId}</code></p></div><code>{failure.traceId}</code></div><ul>{failure.reasons.map((reason)=><li key={reason}>{reason}</li>)}</ul>{observation===undefined?null:<dl><div><dt>期望</dt><dd>{observation.expectedSummary}</dd></div><div><dt>实际</dt><dd>{observation.actualSummary}</dd></div><div><dt>证据</dt><dd><pre>{JSON.stringify(observation.details,null,2)}</pre></dd></div></dl>}</article>})}</section>
  </main>
}

function Metrics({domain,run}:{domain:P9EvalDomain;run:P9EvalRunSummary}){const m=run.metrics;const common=<><Metric label="发布门禁" value={m.releaseGate} bad={m.releaseGate==='FAIL'}/><Metric label="通过率" value={pct(m.passRate)} bad={m.passRate<1}/></>;return <section className="eval-summary-grid" aria-label={`${domain} 指标`}>{common}{domain==='REPORT'?<><Metric label="隐藏字段" value={pct(m.hiddenFieldBlockedRate??0)} bad={(m.hiddenFieldBlockedRate??0)<1}/><Metric label="证据覆盖率" value={pct(m.evidenceCoverageRate??0)} bad={(m.evidenceCoverageRate??0)<1}/><Metric label="活动内容" value={pct(m.activeContentBlockedRate??0)} bad={(m.activeContentBlockedRate??0)<1}/><Metric label="外部链接" value={pct(m.externalLinkBlockedRate??0)} bad={(m.externalLinkBlockedRate??0)<1}/><Metric label="XLSX 宏" value={pct(m.xlsxMacroBlockedRate??0)} bad={(m.xlsxMacroBlockedRate??0)<1}/><Metric label="PDF 动作" value={pct(m.pdfActionBlockedRate??0)} bad={(m.pdfActionBlockedRate??0)<1}/><Metric label="SVG 脚本" value={pct(m.svgScriptBlockedRate??0)} bad={(m.svgScriptBlockedRate??0)<1}/><Metric label="路径遍历" value={pct(m.pathTraversalBlockedRate??0)} bad={(m.pathTraversalBlockedRate??0)<1}/><Metric label="大小" value={pct(m.sizeLimitEnforcedRate??0)} bad={(m.sizeLimitEnforcedRate??0)<1}/><Metric label="超时" value={pct(m.timeoutEnforcedRate??0)} bad={(m.timeoutEnforcedRate??0)<1}/></>:<><Metric label="写入 SQL 拒绝" value={pct(m.writeSqlRejectRate??0)} bad={(m.writeSqlRejectRate??0)<1}/><Metric label="SELECT * 拒绝" value={pct(m.selectStarRejectRate??0)} bad={(m.selectStarRejectRate??0)<1}/><Metric label="LOAD_FILE 拒绝" value={pct(m.loadFileRejectRate??0)} bad={(m.loadFileRejectRate??0)<1}/><Metric label="系统 schema 拒绝" value={pct(m.systemSchemaRejectRate??0)} bad={(m.systemSchemaRejectRate??0)<1}/><Metric label="Python 能力" value={pct(m.pythonCapabilityBlockedRate??0)} bad={(m.pythonCapabilityBlockedRate??0)<1}/><Metric label="动态 queryId" value={pct(m.dynamicQueryIdSafetyRate??0)} bad={(m.dynamicQueryIdSafetyRate??0)<1}/><Metric label="证明" value={pct(m.attestationVerifiedRate??0)} bad={(m.attestationVerifiedRate??0)<1}/><Metric label="载荷预算" value={pct(m.payloadBudgetEnforcedRate??0)} bad={(m.payloadBudgetEnforcedRate??0)<1}/></>}</section>}
function Metric({label,value,bad}:{label:string;value:string;bad:boolean}){return <article data-critical={bad}><strong>{value}</strong><span>{label}</span></article>}
async function loadSnapshot(client:P9ApiClient,domain:P9EvalDomain):Promise<P9EvalSnapshot>{const[cases,runs]=await Promise.all([client.listEvalCases(domain),client.listEvalRuns(domain)]);const broken=`run-p9-${domain.toLowerCase()}-broken-v0`;const selectedRun=runs.find((item)=>item.runId===broken)??runs[0];if(selectedRun===undefined)return{cases,runs,observations:[],failures:[]};const[observations,failures]=await Promise.all([client.listEvalObservations(selectedRun.runId),client.listEvalFailures(selectedRun.runId)]);return{cases,runs,selectedRun,observations,failures}}
function datasetId(domain:P9EvalDomain):string{return domain==='REPORT'?'report-safety-v1':'sandbox-safety-v1'}
function title(domain:P9EvalDomain):string{return domain==='REPORT'?'报告评测':'沙箱评测'}
function description(domain:P9EvalDomain):string{return domain==='REPORT'?'隐藏字段、证据覆盖率、活动内容、外部链接、宏/动作/脚本、路径遍历、大小与超时安全。':'只读 SQL、显式列、LOAD_FILE/系统 schema 拒绝、Python 能力限制、动态 queryId、证明与载荷预算安全。'}
function pct(value:number):string{return `${(value*100).toFixed(1)}%`}
function messageOf(reason:unknown):string{return reason instanceof Error?reason.message:String(reason)}
