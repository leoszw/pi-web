import { useEffect, useState } from 'react'
import type {
  BaselineAcceptance,
  ReleaseWaiver,
  UnifiedBenchmarkRun,
  UnifiedCorpusManifest,
  UnifiedReleaseDecision,
  UnifiedRunComparison,
} from '../../../../shared/industry/eval/p11'
import { createP11ApiClient, type P11ApiClient } from '../../api/p11-client'
import './eval.css'

const defaultClient=createP11ApiClient()
export interface UnifiedBenchmarkSnapshot {
  manifest: UnifiedCorpusManifest
  runs: readonly UnifiedBenchmarkRun[]
  baseline: BaselineAcceptance|null
  waivers: readonly ReleaseWaiver[]
  selectedRun?: UnifiedBenchmarkRun
  decision?: UnifiedReleaseDecision
  comparison?: UnifiedRunComparison
}

export function UnifiedBenchmarkPage({client=defaultClient,initialSnapshot}:{client?:P11ApiClient;initialSnapshot?:UnifiedBenchmarkSnapshot}){
  const[snapshot,setSnapshot]=useState<UnifiedBenchmarkSnapshot|undefined>(initialSnapshot)
  const[variant,setVariant]=useState<'p11-guarded-v1'|'p11-broken-v0'>('p11-guarded-v1')
  const[waiverReason,setWaiverReason]=useState('Temporary documented exception while external validation is completed.')
  const[waiverExpiresAt,setWaiverExpiresAt]=useState(()=>new Date(Date.now()+24*60*60*1000).toISOString().slice(0,16))
  const[busy,setBusy]=useState(false)
  const[error,setError]=useState<string|null>(null)
  useEffect(()=>{if(initialSnapshot!==undefined)return undefined;let cancelled=false;void load(client).then((value)=>{if(!cancelled)setSnapshot(value)}).catch((reason:unknown)=>{if(!cancelled)setError(messageOf(reason))});return()=>{cancelled=true}},[client,initialSnapshot])
  async function refresh(selectedRunId?:string){const base=await baseSnapshot(client);const selected=base.runs.find((item)=>item.runId===selectedRunId)??base.runs.find((item)=>item.runId==='run-p11-candidate-v2')??base.runs[0];const decision=selected===undefined?undefined:await client.getDecision(selected.runId);const comparison=base.baseline===null||selected===undefined||base.baseline.runId===selected.runId?undefined:await client.compare(base.baseline.runId,selected.runId);setSnapshot({...base,selectedRun:selected,decision,comparison})}
  async function select(run:UnifiedBenchmarkRun){setBusy(true);setError(null);try{const decision=await client.getDecision(run.runId);const comparison=snapshot?.baseline===null||snapshot?.baseline===undefined||snapshot.baseline.runId===run.runId?undefined:await client.compare(snapshot.baseline.runId,run.runId);setSnapshot((value)=>value===undefined?value:{...value,selectedRun:run,decision,comparison})}catch(reason){setError(messageOf(reason))}finally{setBusy(false)}}
  async function start(){if(snapshot===undefined)return;setBusy(true);setError(null);try{const run=await client.startRun({corpusId:snapshot.manifest.corpusId,variantId:variant});await refresh(run.runId)}catch(reason){setError(messageOf(reason))}finally{setBusy(false)}}
  async function acceptBaseline(){const run=snapshot?.selectedRun;if(run===undefined)return;setBusy(true);setError(null);try{await client.acceptBaseline(run.runId);await refresh(run.runId)}catch(reason){setError(messageOf(reason))}finally{setBusy(false)}}
  async function createWaiver(){const run=snapshot?.selectedRun;if(run===undefined||!validLocalDateTime(waiverExpiresAt))return;setBusy(true);setError(null);try{await client.createWaiver(run.runId,{reason:waiverReason,expiresAt:new Date(waiverExpiresAt).toISOString()});await refresh(run.runId)}catch(reason){setError(messageOf(reason))}finally{setBusy(false)}}
  if(snapshot===undefined)return <main className="eval-page"><h1>Unified Benchmark</h1><p>{error??'Loading unified benchmark…'}</p></main>
  return <UnifiedBenchmarkView snapshot={snapshot} variant={variant} waiverReason={waiverReason} waiverExpiresAt={waiverExpiresAt} busy={busy} error={error} onVariant={setVariant} onWaiverReason={setWaiverReason} onWaiverExpiresAt={setWaiverExpiresAt} onStart={()=>void start()} onSelect={(run)=>void select(run)} onAcceptBaseline={()=>void acceptBaseline()} onCreateWaiver={()=>void createWaiver()}/>
}

export function UnifiedBenchmarkView({snapshot,variant,waiverReason,waiverExpiresAt,busy,error,onVariant=()=>undefined,onWaiverReason=()=>undefined,onWaiverExpiresAt=()=>undefined,onStart=()=>undefined,onSelect=()=>undefined,onAcceptBaseline=()=>undefined,onCreateWaiver=()=>undefined}:{snapshot:UnifiedBenchmarkSnapshot;variant:'p11-guarded-v1'|'p11-broken-v0';waiverReason:string;waiverExpiresAt:string;busy:boolean;error:string|null;onVariant?:(value:'p11-guarded-v1'|'p11-broken-v0')=>void;onWaiverReason?:(value:string)=>void;onWaiverExpiresAt?:(value:string)=>void;onStart?:()=>void;onSelect?:(run:UnifiedBenchmarkRun)=>void;onAcceptBaseline?:()=>void;onCreateWaiver?:()=>void}){
  const run=snapshot.selectedRun??snapshot.runs[0]
  const decision=snapshot.decision
  const activeWaiver=decision?.waiver
  return <main className="eval-page" aria-labelledby="p11-title">
    <div className="eval-eyebrow">Unified Benchmark / Release Gate · P11</div>
    <div className="eval-heading-row"><div><h1 id="p11-title">Unified Benchmark</h1><p>Phase 11 corpus, explicit Baseline Accept, comparison evidence, and release-gate evidence from the configured evaluation adapter. The browser never computes or overrides Gate status.</p></div><div><strong>{snapshot.manifest.totalCaseCount}</strong> cases · <code>{snapshot.manifest.version}</code></div></div>
    {error===null?null:<p role="alert">{error}</p>}

    <section className="eval-panel"><h2>Corpus Manifest</h2><div className="eval-summary-grid"><article><strong>{snapshot.manifest.totalCaseCount}</strong><span>Golden / Hard</span></article><article><strong>{snapshot.manifest.goldenCount}</strong><span>Golden</span></article><article><strong>{snapshot.manifest.hardCount}</strong><span>Hard</span></article><article><strong>{snapshot.manifest.criticalCaseCount}</strong><span>Critical E2E</span></article></div><p>Status: <strong>{snapshot.manifest.status}</strong> · Full coverage required: <strong>{snapshot.manifest.fullCoverageRequired?'YES':'NO'}</strong> · Source: <code>{snapshot.manifest.source}</code></p><p>Fingerprint: <code>{snapshot.manifest.fingerprint}</code></p><table className="eval-table"><thead><tr><th>Domain</th><th>Cases</th></tr></thead><tbody>{snapshot.manifest.domainCounts.map((item)=><tr key={item.domain}><td>{item.domain}</td><td>{item.caseCount}</td></tr>)}</tbody></table></section>

    <section className="eval-panel"><div className="eval-heading-row"><div><h2>Benchmark Runs</h2><p>Guarded runs model complete/reproducible release evidence; broken runs exercise fail-closed paths.</p></div><div><select aria-label="Benchmark profile" value={variant} disabled={busy} onChange={(event)=>onVariant(event.target.value as 'p11-guarded-v1'|'p11-broken-v0')}><option value="p11-guarded-v1">p11-guarded-v1</option><option value="p11-broken-v0">p11-broken-v0</option></select> <button type="button" disabled={busy} onClick={onStart}>Run Unified Benchmark</button></div></div><table className="eval-table"><thead><tr><th>Run</th><th>Coverage</th><th>Critical E2E</th><th>Gate</th><th>Reproducible</th><th>Baseline</th><th /></tr></thead><tbody>{snapshot.runs.map((item)=><tr key={item.runId} data-selected={run?.runId===item.runId}><td><code>{item.runId}</code></td><td>{(item.coverageRate*100).toFixed(1)}% ({item.coveredCaseCount}/1030)</td><td>{item.criticalE2E.passed?'PASS':'FAIL'} · {item.criticalE2E.passedCount}/{item.criticalE2E.total}</td><td><strong>{item.releaseGate.status}</strong> · source {item.releaseGate.source}</td><td>{item.reproducibility.complete?'complete':'incomplete'}</td><td>{snapshot.baseline?.runId===item.runId?'ACCEPTED':'—'}</td><td><button type="button" disabled={busy} onClick={()=>onSelect(item)}>Inspect</button></td></tr>)}</tbody></table></section>

    {run===undefined?null:<section className="eval-panel"><div className="eval-heading-row"><div><h2>Release Gate</h2><p><code>{run.runId}</code></p></div><strong>{decision?.releaseDisposition??run.releaseGate.status}</strong></div><p>{run.releaseGate.source==='PI'?'PI Gate':'Mock PI Gate'}: <strong>{run.releaseGate.status}</strong> · source <strong>{run.releaseGate.source}</strong> · rule <code>{run.releaseGate.ruleVersion}</code></p>{run.releaseGate.reasons.length===0?<p>No gate failures.</p>:<ul>{run.releaseGate.reasons.map((reason)=><li key={reason}>{reason}</li>)}</ul>}<p>Coverage: {run.coveredCaseCount}/1030 · Dataset reviewed: {run.datasetReviewed?'yes':'no'} · Critical E2E: {run.criticalE2E.passed?'PASS':'FAIL'} · Reproducibility: {run.reproducibility.complete?'complete':'incomplete'}</p>{run.reproducibility.missing.length===0?null:<p>Missing reproducibility: {run.reproducibility.missing.join(', ')}</p>}<p>Baseline eligible: <strong>{decision?.baselineEligible?'YES':'NO'}</strong></p>{decision?.baselineBlockReasons.length?<ul>{decision.baselineBlockReasons.map((reason)=><li key={reason}>{reason}</li>)}</ul>:null}{decision?.baselineEligible&&snapshot.baseline?.runId!==run.runId?<button type="button" disabled={busy} onClick={onAcceptBaseline}>Explicitly Accept as Baseline</button>:null}<p><strong>No Manual PASS.</strong> The UI cannot mutate the adapter-owned Gate. A Waiver is a separate exception record and the original Gate remains FAIL.</p><h3>Metrics</h3><table className="eval-table"><thead><tr><th>Metric</th><th>Value</th><th>Regression threshold</th><th>Critical</th></tr></thead><tbody>{run.metrics.map((item)=><tr key={item.metricId}><td>{item.label}<br/><code>{item.metricId}</code></td><td>{item.value.toFixed(4)}</td><td>{item.regressionThreshold.toFixed(4)}</td><td>{item.critical?'yes':'no'}</td></tr>)}</tbody></table></section>}

    {run?.releaseGate.status==='FAIL'&&activeWaiver===undefined?<section className="eval-panel"><h2>Create Waiver</h2><p>Waiver does not change Gate=FAIL and cannot make this run baseline eligible.</p><label>Reason<textarea value={waiverReason} disabled={busy} onChange={(event)=>onWaiverReason(event.target.value)}/></label><label>Expires<input type="datetime-local" value={waiverExpiresAt} disabled={busy} onChange={(event)=>onWaiverExpiresAt(event.target.value)}/></label><button type="button" disabled={busy||waiverReason.trim().length<20||!validLocalDateTime(waiverExpiresAt)} onClick={onCreateWaiver}>Create Separate Waiver</button></section>:null}

    <section className="eval-panel"><h2>Current Baseline</h2>{snapshot.baseline===null?<p>No baseline accepted.</p>:<p><code>{snapshot.baseline.runId}</code> · explicit={String(snapshot.baseline.explicit)} · accepted by {snapshot.baseline.acceptedBy} · {snapshot.baseline.acceptedAt}</p>}</section>

    {snapshot.comparison===undefined?null:<section className="eval-panel"><h2>Compare</h2><p><code>{snapshot.comparison.baselineRunId}</code> → <code>{snapshot.comparison.candidateRunId}</code></p><h3>Metric delta / threshold</h3><table className="eval-table"><thead><tr><th>Metric</th><th>Baseline</th><th>Candidate</th><th>Delta</th><th>Threshold</th><th>Regression</th></tr></thead><tbody>{snapshot.comparison.metricDeltas.map((item)=><tr key={item.metricId}><td>{item.label}</td><td>{item.baseline.toFixed(4)}</td><td>{item.candidate.toFixed(4)}</td><td>{item.delta.toFixed(4)}</td><td>{item.regressionThreshold.toFixed(4)}</td><td>{item.regressed?'YES':'NO'}</td></tr>)}</tbody></table><h3>Improved / regressed cases</h3><ul>{snapshot.comparison.caseMovements.map((item)=><li key={item.caseId}><strong>{item.movement}</strong> · {item.domain} · {item.caseId} · {item.title} · {item.baselineScore}→{item.candidateScore}</li>)}</ul><h3>Version diff</h3><ul>{snapshot.comparison.versionDiff.map((item)=><li key={item.component}>{item.component}: <code>{item.before}</code> → <code>{item.after}</code> · changed={String(item.changed)}</li>)}</ul><h3>Rank movement</h3><ul>{snapshot.comparison.rankMovements.map((item)=><li key={`${item.caseId}-${item.entityId}`}>{item.caseId} · entity <code>{item.entityId}</code> · rank {item.beforeRank}→{item.afterRank} · Δ {item.delta}</li>)}</ul><h3>Statistical CI</h3><p>{snapshot.comparison.statisticalCI.method} · 95% CI [{snapshot.comparison.statisticalCI.lower.toFixed(4)}, {snapshot.comparison.statisticalCI.upper.toFixed(4)}] · n={snapshot.comparison.statisticalCI.sampleCount} · {snapshot.comparison.statisticalCI.conclusive?'CONCLUSIVE':'INCONCLUSIVE'}</p></section>}

    <section className="eval-panel"><h2>Waivers</h2>{snapshot.waivers.length===0?<p>No waivers.</p>:<table className="eval-table"><thead><tr><th>Run</th><th>Status</th><th>Original Gate</th><th>Disposition</th><th>Reason</th><th>Expires</th></tr></thead><tbody>{snapshot.waivers.map((item)=><tr key={item.waiverId}><td><code>{item.runId}</code></td><td>{item.status}</td><td>{item.originalGate}</td><td>{item.releaseDisposition}</td><td>{item.reason}</td><td>{item.expiresAt}</td></tr>)}</tbody></table>}</section>
  </main>
}

async function baseSnapshot(client:P11ApiClient){const[manifest,runs,baseline,waivers]=await Promise.all([client.getCorpus(),client.listRuns(),client.getBaseline(),client.listWaivers()]);return{manifest,runs,baseline,waivers}}
async function load(client:P11ApiClient):Promise<UnifiedBenchmarkSnapshot>{const base=await baseSnapshot(client);const selected=base.runs.find((item)=>item.runId==='run-p11-candidate-v2')??base.runs[0];const decision=selected===undefined?undefined:await client.getDecision(selected.runId);const comparison=base.baseline===null||selected===undefined||base.baseline.runId===selected.runId?undefined:await client.compare(base.baseline.runId,selected.runId);return{...base,selectedRun:selected,decision,comparison}}
function validLocalDateTime(value:string):boolean{return value.trim()!==''&&Number.isFinite(Date.parse(value))}
function messageOf(reason:unknown):string{return reason instanceof Error?reason.message:String(reason)}
